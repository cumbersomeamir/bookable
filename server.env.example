# Server Environment Configuration Example
# Copy this to server.env and fill in your values

NODE_ENV=development

# Server Port
PORT=4000

# MongoDB Connection
# Replace with your MongoDB connection string
# Collections will use "bookable-" prefix (e.g., bookable-users, bookable-books)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bookable?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=*

